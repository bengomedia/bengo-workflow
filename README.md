
## What this is

A single HTML file (`index.html`) hosted on GitHub Pages, backed by a Supabase PostgreSQL database. No frameworks, no build process, no dependencies to install. Just a file and a database.

- **Frontend:** Plain HTML/CSS/JavaScript — one file, opens in any browser
- **Database:** Supabase (PostgreSQL) — free tier, hosted in the cloud
- **Hosting:** GitHub Pages — free, updates automatically when you push a new file

---

## Key credentials

Keep these somewhere safe (not in this file once it's in GitHub).

| Thing | Value |
|---|---|
| Supabase project URL | `https://uxgspzloagefqqvqfqlw.supabase.co` |
| Supabase dashboard | https://supabase.com → sign in → bengomedia-workflow |
| GitHub repo | your GitHub account → bengo-workflow |
| Live app URL | `https://[yourusername].github.io/bengo-workflow` |

---

## How to update the app

1. Make changes here with Claude (just paste the conversation link or start a new one and explain what you want)
2. Download the new `bengo-workflow.html` file
3. Rename it to `index.html`
4. Go to your GitHub repo → click `index.html` → click the pencil icon → drag and drop the new file, or use **Add file > Upload files**
5. Commit the change
6. Wait 1-2 minutes — the live URL updates automatically

That's the whole deployment process.

---

## Database structure

Five tables in Supabase. You can view and edit data directly in the Supabase Table Editor if needed.

### `people`
| Column | Type | Notes |
|---|---|---|
| id | text | Unique ID (generated by app) |
| name | text | Full name |
| email | text | |
| role | text | Producer / Editor / Executive Producer / Accounts / Admin |
| user_role | text | User or Admin (app display only) |
| active | boolean | |

### `podcasts`
| Column | Type | Notes |
|---|---|---|
| id | text | |
| name | text | Show name |
| company | text | Client company |
| contact | text | Client contact name |
| type | text | Interview / Solo / Panel / Scripted |
| ep | text | FK → people.id (Executive Producer) |
| producer | text | FK → people.id |
| editor | text | FK → people.id |
| accounts | text | FK → people.id |
| season | text | |
| episode_count | integer | |
| frequency | text | Weekly / BiWeekly / Monthly |

### `episodes`
| Column | Type | Notes |
|---|---|---|
| id | text | |
| podcast_id | text | FK → podcasts.id (cascades on delete) |
| number | integer | Episode number |
| title | text | Custom title (editable in app) |
| default_title | text | Auto-generated S01E01 format |
| launch_date | date | |
| status | text | |

### `tasks`
| Column | Type | Notes |
|---|---|---|
| id | text | |
| episode_id | text | FK → episodes.id (cascades on delete) |
| name | text | Task name |
| role | text | Role responsible |
| assigned_to | text | FK → people.id |
| status | text | To Do / In Progress / Waiting / Blocked / Complete |
| due_date | date | Calculated from launch date - days before |
| start_date | date | Optional |
| task_order | integer | Display order |
| notes | text | |

### `todos`
| Column | Type | Notes |
|---|---|---|
| id | text | |
| name | text | |
| assigned_to | text | FK → people.id |
| podcast_id | text | FK → podcasts.id (optional, sets null on delete) |
| due_date | date | |
| status | text | |
| notes | text | |

---

## Task templates

The task templates live **in the app code** (not in the database). They're defined in the `TEMPLATES` object in the JavaScript. Each podcast type (Interview, Solo, Panel, Scripted) has its own list of tasks with:

- `name` — task name
- `role` — which role gets assigned
- `order` — display order
- `days` — how many days before launch date the task is due

To add, remove or rename a task from the template, you need to update the code. Come back here with Claude and ask for the change — it takes about 2 minutes.

---

## Current task template (Interview — all types are similar)

| Order | Task | Role | Days before launch |
|---|---|---|---|
| 10 | Recording Studio Booked | Producer | 14 |
| 11 | Guest Booked | Producer | 14 |
| 20 | Podcast Recorded | Producer | 10 |
| 30 | Episode Title Written | Producer | 9 |
| 40 | Episode Description Written | Producer | 9 |
| 50 | Podcast Edited | Editor | 8 |
| 60 | Podcast Exec'd | Executive Producer | 7 |
| 70 | Sent to Client | Executive Producer | 7 |
| 80 | Social Media Clips Produced | Producer | 7 |
| 90 | Social Media Clips Exec'd | Executive Producer | 6 |
| 100 | Social Media Clips Client Approved | Producer | 5 |
| 110 | Podcast Scheduled | Executive Producer | 1 |
| 120 | Podcast Launched | Executive Producer | 0 |
| 125 | EP Confirms Invoice Amount | Executive Producer | 0 |
| 130 | Invoice Raised | Accounts | 0 |
| 135 | Payment Received | Accounts | 0 |

---

## If something breaks

**App won't load / "Could not connect" message**
- Check your internet connection
- Check Supabase is up: https://status.supabase.com
- Check the API key in the code matches what's in Supabase Settings > API Keys

**Data looks wrong or missing**
- Go to Supabase → Table Editor and check the data directly
- You can edit records manually in there if needed

**Someone accidentally deleted something**
- Supabase keeps point-in-time backups on paid plans; the free tier doesn't have this
- Worth upgrading to Pro ($25/month) if the data becomes business-critical

**Need to add a new role to the task templates**
- Come back to Claude with this README and ask for the change
- Explain which role, which tasks, and which podcast types it applies to

---

## Future things worth considering

- **Proper login/auth** — right now anyone with the URL can read and write data. Fine for internal use, but worth locking down if the URL ever gets shared outside the team. Supabase has built-in auth that Claude can wire up.
- **Email notifications** — Supabase can trigger emails when tasks are assigned or overdue. Doable but needs a bit more work.
- **Supabase Pro** — $25/month gets you backups, more storage, and better performance. Not needed yet but worth it once this becomes genuinely business-critical.
- **Mobile view** — the app works on mobile but isn't optimised for it. Fixable if the team start using it on phones.

---

## Updating this README

Keep this file updated when you make changes to the app. Specifically update the task template table and the database structure section if those change.

Both docs (`README.md` for you, `TEAM-GUIDE.md` for the team) live in the same GitHub repo as `index.html`.

---

*Last updated: March 2026*

